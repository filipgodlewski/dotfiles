echo "venv-update -- Update packages in the given virtual environment.

Description:
  If no VENV provided, will update all system-site packages to the newest version,
  unless you are currently on an active virtual environment.
  In such case, the packages of that particular venv will be updated.

Usage:
  venv update [VENV] [options]

Options:
  -h, --help                  Show this help message.
  -q, --quiet                 Supress messages.
  --dry-run                   Do not execute the command, but rather check what would the outcome be.
"

# vim:ft=zsh
