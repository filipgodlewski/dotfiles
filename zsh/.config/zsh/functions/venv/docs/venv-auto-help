echo "venv-auto -- Automate activating venv on chpwd.

Description:
  Should only be used within the chpwd(). An example would be:

  chpwd() {
    venv auto
  }

  Which effectively means, whenever 'cd' command is being executed, run 'venv auto'.

Usage:
  venv auto

Options:
  -h, --help                  Show this help message.
"

# vim:ft=zsh
